<div class="container">
    <div class="row">
        <div class="col-xs-offset-2 col-xs-8">
            <%= form_with(url: timelines_path, local: true) do |f| %>
                <%= f.text_area :content, class:'form-controll' %>
                <%= f.submit '>', class:'btn btn-primary'%>
            <% end %>
        </div>
    </div>
</div>