<div class="container">
    <div class="row">
        <div class="col-xs-6">
            <%= @other_user.name %>
        </div>

        <div id='room' class="container messages" data-room_id='<%= @room.id %>' data-current_user='<%= current_user.id%>' data-other_user='<%= @other_user.id %>'>
            <div id="chat_window">
                <%= render 'messages/message' %>
            </div>
        </div>
        <div class="container">
            <%= render 'form' %>
        </div>
    </div>
</div>
